return {
  get_root = require('user.utils.get_root').get_root,
  telescope = require('user.utils.telescope').telescope,
  has_words_before = require('user.utils.has_words_before').has_words_before,
  toggle_indent = require('user.utils.toggle_indent').toggle_indent,
  format = require('user.utils.format').format,
  toggle_autoformat = require('user.utils.format').toggle_autoformat,
  random_string = require('user.utils.random_string').random_string,
}
